# Landy API 서버 베이스 URL — 브라우저는 /api 프록시(next.config rewrite)를 경유하고,
# 이 값은 서버(SSR)와 rewrite 대상에서만 사용됨 (변수명의 NEXT_PUBLIC_은 기존 명명 유지)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# 배포 환경 식별자: prod | dev | local — 탭 제목/파비콘/상단 스트립/헤더 배지 색상을 결정
# Vercel의 admin-prod 프로젝트에는 prod, admin-dev 프로젝트에는 dev로 설정할 것
# (미설정 시 local로 간주)
NEXT_PUBLIC_APP_ENV=local

# --- Kakao OAuth (서버 전용 — 절대 NEXT_PUBLIC_ 접두사 붙이지 말 것) ---
# Kakao 개발자 콘솔의 REST API 키 (authorize + 토큰 교환의 client_id)
KAKAO_REST_API_KEY=
# Kakao 콘솔에서 "Client Secret"을 활성화한 경우에만 설정 (선택)
KAKAO_CLIENT_SECRET=
# Kakao 앱에 등록한 Redirect URI와 정확히 일치해야 함
# 로컬: http://localhost:3000/auth/kakao/callback
# 운영: https://<admin-host>/auth/kakao/callback
KAKAO_REDIRECT_URI=http://localhost:3000/auth/kakao/callback
